// Input radius and compute the area of a circle
#include <iostream>
using namespace std;

int main() {
    cout << "Input the radius: ";
    int radius;
    cin >> radius;
    int area;
    area = 3*radius*radius;
    cout << "The area of the circle is: " << area << endl;
}